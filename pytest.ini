[pytest]
testpaths = tests
addopts = -ra --strict-markers -m "not integration"
markers =
    integration: testes que tocam o sistema real (WMI, PowerShell, registro, rede). Pulados por padrão. Rode com `pytest -m integration` para executá-los.
